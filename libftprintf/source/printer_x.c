#include <stdarg.h>
#include "type.h"

#include <stdio.h>

int ft_print_X(t_args *sarg, va_list *larg)
{
	printf("In ft_print_X\n");
	if (!sarg && !larg)
		return (-1);
	return (-1);
}

int ft_print_x(t_args *sarg, va_list *larg)
{
	printf("In ft_print_x\n");
	if (!sarg && !larg)
		return (-1);
	return (-1);
}